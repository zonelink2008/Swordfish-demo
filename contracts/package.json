{
  "name": "swordfish-contracts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Swordfish ERC-3643 智能合约",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "@openzeppelin/contracts": "^4.9.6",
    "hardhat": "^2.28.6"
  },
  "dependencies": {
    "@ERC3643/reference-contracts": "^1.0.0"
  }
}
